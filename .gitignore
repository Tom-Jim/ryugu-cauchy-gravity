# Build outputs
/target
/pkg
/bakes/build
/bakes/esa-build
/node_modules

# Vendored ESA polyhedral-gravity library (local checkout, not part of the repo)
/vendor/polyhedral-gravity-model

# Runtime bake state: resume checkpoints, face orders, progress logs. The
# finished `assets/records/*_faces.bin` records are kept.
assets/records/.*
*.log
.DS_Store
