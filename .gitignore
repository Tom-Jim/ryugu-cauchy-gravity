# ---------------------------------------------------------------------------
# Rust / WebAssembly build output
# ---------------------------------------------------------------------------
/target/
/pkg/
bakes/rtfp/target/
bakes/esa-build/
bakes/build/

# wgpu / shaderc scratch
*.spv

# ---------------------------------------------------------------------------
# Node / Bun
# ---------------------------------------------------------------------------
/node_modules/
bun.lockb
*.tsbuildinfo

# ---------------------------------------------------------------------------
# Third-party checkouts and downloaded meshes
#
# `vendor/polyhedral-gravity-model` is the ESA reference library, fetched
# locally by `bun run esa:configure`; `assets/models/*.obj` are Hayabusa2 shape
# models that are far too large to version. The rendered GLB *is* tracked.
# ---------------------------------------------------------------------------
/vendor/
assets/models/*.obj

# ---------------------------------------------------------------------------
# Bake scratch state
#
# Face orderings, resume checkpoints and progress logs are regenerated by every
# run. The finished `assets/records/*_faces.bin` records are tracked: they are
# the comparison artefacts the viewer and the README figures are built from.
# ---------------------------------------------------------------------------
assets/records/.*
assets/records/*.log

# ---------------------------------------------------------------------------
# Environment / editor / OS
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*~
